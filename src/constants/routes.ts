const routes = {
  addMatch: '/add',
  editMatch: '/edit/:id',
  home: '/',
  matchDetail: '/matches/:id',
  matchList: '/matches',
  register: '/register',
  resetPassword: '/reset',
  settings: '/settings',
};

export default routes;
